<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="res_config_settings_view_form_sale" model="ir.ui.view">
        <field
            name="name"
        >res.config.settings.view.form.inherit.weighbridge.jwt.api</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="55" />
        <field
            name="inherit_id"
            ref="wi_base_weighbridge.res_config_settings_view_form"
        />
        <field name="arch" type="xml">
            <xpath expr="//block[@name='module_setting_container']" position="inside">
                <setting
                    string="Allow Update Scale Ticket"
                    help="Allow the system to perform update when receiving data from the API."
                    id="allow_update_scale_ticket"
                >
                    <field name="allow_update_scale_ticket" />
                </setting>
                <setting
                    string="Auto Posting Scale Ticket"
                    help="Allow the system to perform auto-posting when receiving data from the API. This will prevent data from being updated via the API."
                    id="auto_post_scale_ticket"
                >
                    <field name="auto_post_scale_ticket" />
                </setting>
            </xpath>
        </field>
    </record>

</odoo>
